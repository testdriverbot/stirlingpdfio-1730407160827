version: 4.0.78
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Assert that the webpage loads.
    commands:
      - command: assert
        expect: the text "Stirling PDF" is visible on the webpage
  - prompt: >-
      Assert that the Stirling PDF logo is visible.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: the text "Stirling PDF" is visible on the webpage
